import {describe,it,expect} from 'vitest';import {assets,MINIMUM_USD} from './assets';describe('asset configuration',()=>{it('enforces a ten-thousand-dollar floor',()=>expect(MINIMUM_USD).toBe(10000));it('keeps networks explicit',()=>{expect(assets.ETH.network).toContain('Chain 1');expect(assets.BNB.network).toContain('Chain 56');expect(assets.POL.network).toContain('Chain 137')})});
