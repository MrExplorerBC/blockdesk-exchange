'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const router=useRouter();async function go(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});if(r.ok)router.push('/dashboard');else setError('Invalid email or password.')}return <main className="mx-auto max-w-md px-6 py-20"><form className="glass p-8" onSubmit={go}><div className="label">Secure access</div><h1 className="my-3 text-3xl font-black">Sign in</h1><p className="mb-7 text-sm text-emerald-100/60">Administrative access is protected by an eight-hour encrypted session.</p><label className="label">Email<input className="field mt-2 mb-5 normal-case" type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="label">Password<input className="field mt-2 mb-6 normal-case" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} minLength={12} required/></label><button className="btn w-full">Sign in</button>{error&&<p className="mt-4 text-sm text-red-300" role="alert">{error}</p>}</form></main>}
