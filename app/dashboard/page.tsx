import {redirect} from 'next/navigation'; import {getSession} from '@/lib/auth';
export default async function Dashboard(){const session=await getSession();if(!session)redirect('/login');return <main className="mx-auto max-w-6xl px-6 py-14"><div className="label">Operations</div><h1 className="mt-2 text-4xl font-black">Order dashboard</h1><div className="glass mt-8 p-8"><p className="text-emerald-100/70">Signed in as {String(session.email)}.</p><div className="mt-6 rounded-xl border border-amber-500/30 p-4 text-sm text-amber-200">Mainnet deposit display is locked. Configure the production database, verified custody provider, compliance provider, and deposit-monitor workers before activation.</div></div></main>}
